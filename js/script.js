const swiper = new Swiper('.swiper-container', {
    // Optional parameters
    loop: true,
    effect: 'slide',

    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  
  });